# protobuf-text-config

Example of using protocol buffer Text Format Language in a configuration file.

# Run

```shell
$ make protoc
$ go run main.go
verbose: true
example.com provider https://example.com
github.com provider https://github.com
```
